# PROJECT

## Commands
```bash
make setup
make dev
make test
make lint
make typecheck
make build
make check
make release
```

## Environments
- Python 3.11+ recommended.

## Next 3 improvements
1. Wire remote agent client into the campaign engine with opt-in config.
2. Add scope allowlists to campaign spec and enforce in modules.
3. Emit JSON schema files for campaign and evidence packs.
